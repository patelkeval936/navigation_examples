import 'package:flutter/material.dart';

class Organisation extends StatelessWidget {
  const Organisation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dashboard > Organisation'),
    );
  }
}
