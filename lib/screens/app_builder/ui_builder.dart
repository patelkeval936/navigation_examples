import 'package:flutter/material.dart';

class UIBuilder extends StatelessWidget {
  const UIBuilder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('App Builder > Navigation Builder > UI Builder'),);
  }
}
